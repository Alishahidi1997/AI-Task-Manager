[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    live_openai: calls OpenAI (requires EVAL_LIVE=1 and OPENAI_API_KEY)
addopts = -m "not live_openai"
